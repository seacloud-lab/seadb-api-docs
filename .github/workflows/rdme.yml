# Sync the SeaDB OpenAPI definition to ReadMe.
name: ReadMe GitHub Action

on:
  push:
    branches:
      - main
    paths:
      - api/seadb.yaml
      - reference/introduction.md
      - .github/workflows/rdme.yml
  workflow_dispatch:

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Upload SeaDB OpenAPI document
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./api/seadb.yaml --key=${{ secrets.README_API_KEY }}

      - name: Upload SeaDB introduction page
        uses: readmeio/rdme@v10
        with:
          rdme: reference upload ./reference/introduction.md --key=${{ secrets.README_API_KEY }}
